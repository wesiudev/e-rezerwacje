import Link from "next/link";

export default function WeekMenu({
  weekMenu,
  setWeekMenu,
  userHours,
}: {
  weekMenu: number;
  setWeekMenu: Function;
  userHours: any;
}) {
  return <div></div>;
}
